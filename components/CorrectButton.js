import Icon from 'react-native-vector-icons/MaterialIcons';
import colors from '../constants/colors';

export default function CorrectButton() {
  return <Icon name='spellcheck' size={20} color={colors.grey} />;
}
